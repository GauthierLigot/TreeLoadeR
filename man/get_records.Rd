% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_records.R
\name{get_records}
\alias{get_records}
\title{Build a Joined Record Table from the Imported Database}
\usage{
get_records(db)
}
\arguments{
\item{db}{A named list (e.g., from \code{get_db()} or \verb{get_saveddb_*()})
containing data frames (or tibbles) named:
\code{record}, \code{calibration}, \code{dendrometer}, \code{tree}, \code{species}, and \code{site}.}
}
\value{
A data frame (typically a tibble) of records joined with
calibration, dendrometer, tree, species, and site information.
}
\description{
Joins the \code{record} table with related metadata tables
(\code{calibration}, \code{dendrometer}, \code{tree}, \code{species}, \code{site})
to produce a single, enriched data frame with commonly used fields.
}
\details{
\itemize{
\item Uses \code{dplyr::left_join()} with \code{dplyr::join_by()} on the appropriate keys.
\item Selects a subset of columns from \code{calibration} (\code{id_calibration:span0})
and from \code{dendrometer} (\code{id_dendrometer:dendrometer_code}).
\item Drops the \code{comment} column from \code{tree} before joining.
}
}
\examples{
\dontrun{
db <- get_db(con)               # or get_saveddb_rds("path")
records <- getRecordTable(db)
dplyr::glimpse(records)
}

}
