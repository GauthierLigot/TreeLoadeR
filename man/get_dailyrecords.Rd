% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_dailyrecords.R
\name{get_dailyrecords}
\alias{get_dailyrecords}
\title{Build an Intra-Annual Record Table (Daily Unique by Calibration)}
\usage{
get_dailyrecords(db)
}
\arguments{
\item{db}{A named list containing tables \code{record}, \code{calibration},
\code{dendrometer}, \code{tree}, \code{species}, and \code{site} (e.g., from \code{get_db()}).}
}
\value{
A data frame/tibble of intra-annual records, with one row per
\verb{(id_calibration, day)} pair and a \code{day} column (numeric days since the
reference date).
}
\description{
Joins record data with related tables and computes a \code{day} index
since a fixed reference date. For each \verb{(id_calibration, day)} pair,
the function keeps the first row (to ensure one observation per day per calibration).
}
\details{
\itemize{
\item The reference date is set in the code as \code{"2024-01-01"}. Adjust as needed.
\item The \code{day} value is computed via \code{difftime(datetime, "2024-01-01", units = "days")}
and then converted to numeric.
\item The function groups by \code{id_calibration} and \code{day}, and keeps the first row
via \code{dplyr::slice(1)}, then ungroups.
}
}
\examples{
\dontrun{
ia <- getIntraAnnualRecord(db)
dplyr::count(ia, id_calibration, day)
}

}
